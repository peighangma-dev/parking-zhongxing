server:
  port: 8090

spring:
  application:
    name: parking-tenant
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/parking_tenant?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123
  redis:
    host: localhost
    port: 6379

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
